-- this file is used to setup one liner extensions
-- larger extesions will be setup in their own files


-- colorbuddy scheme
require'colorbuddy'.colorscheme('gloombuddy')
require'lualine'.setup{}

-- terminal setup
-- require("terminal").setup({
--     layout = { open_cmd = "botright new" },
--     cmd = { vim.o.shell },
--     autoclose = false,
-- })
